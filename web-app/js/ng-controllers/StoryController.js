/*global $:true, _:true, angular:true */
angular.module('thegame').controller('StoryController', [
    '$scope', '$http', '$location', '$window',
    function ($scope, $http, $location, $window) {

        $scope.onInit = function() {
        };
    }
]);
